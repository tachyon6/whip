{
  "name": "whip-agent-plugin",
  "version": "0.1.0",
  "private": true,
  "description": "A focused agent skill for lean, clear, high-compliance prompts.",
  "license": "MIT",
  "scripts": {
    "test": "node --test tests/*.test.mjs"
  }
}
